<?php
/**
 * SHIELD - Page de recuperation de mot de passe
 */
$pageTitle = __('auth.forgot_password_title') . ' - SHIELD';
$bodyClass = 'page-forgot-password';
$pageScripts = ['auth/forgot-password.js'];
?>

<div class="auth-container">
    <!-- Logo -->
    <div class="auth-logo">
        <div class="shield-icon">
            <svg viewBox="0 0 100 100" class="shield-svg">
                <path d="M50 5 L90 20 L90 45 C90 70 70 90 50 95 C30 90 10 70 10 45 L10 20 Z"
                      fill="var(--primary)" stroke="var(--primary-light)" stroke-width="2"/>
                <path d="M50 25 L45 55 L55 55 L50 75"
                      fill="none" stroke="white" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </div>
        <h1 class="auth-title">SHIELD</h1>
        <p class="auth-subtitle"><?= __('auth.forgot_password_subtitle') ?></p>
    </div>

    <!-- Formulaire de recuperation -->
    <form id="forgot-password-form" class="auth-form" novalidate>
        <input type="hidden" name="_csrf" value="<?= $csrfToken ?? '' ?>">

        <p class="text-secondary text-center mb-21">
            <?= __('auth.forgot_password_instructions') ?>
        </p>

        <div class="form-group">
            <label for="email"><?= __('auth.email') ?></label>
            <input type="email"
                   id="email"
                   name="email"
                   class="form-input"
                   placeholder="<?= __('auth.email_placeholder') ?>"
                   autocomplete="email"
                   required>
            <span class="form-error" id="email-error"></span>
        </div>

        <button type="submit" class="btn btn-primary btn-block" id="btn-submit">
            <span class="btn-text"><?= __('auth.send_reset_link') ?></span>
            <span class="btn-loader hidden">
                <svg class="spinner" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="3" fill="none" stroke-dasharray="31.4 31.4"/></svg>
            </span>
        </button>
    </form>

    <!-- Lien retour connexion -->
    <p class="auth-footer">
        <a href="<?= base_url('auth/login') ?>" class="d-inline-flex align-items-center gap-8">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                <path d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/>
            </svg>
            <?= __('auth.back_to_login') ?>
        </a>
    </p>
</div>

<!-- Message de succes -->
<div id="success-message" class="auth-alert hidden" role="alert" style="border-color: var(--success); background-color: rgba(76, 175, 80, 0.15); color: var(--success);">
    <span class="alert-message"><?= __('auth.reset_email_sent') ?></span>
    <button type="button" class="alert-close" aria-label="<?= __('ui.close') ?>">&times;</button>
</div>

<!-- Message d'erreur global -->
<div id="auth-alert" class="auth-alert hidden" role="alert">
    <span class="alert-message"></span>
    <button type="button" class="alert-close" aria-label="<?= __('ui.close') ?>">&times;</button>
</div>
