<?php
/**
 * SHIELD - Gestion des contacts de confiance
 */
$pageTitle = __('contacts.title') . ' - SHIELD';
$bodyClass = 'page-contacts';
$pageScripts = ['app/contacts.js'];
?>

<div class="app-container">
    <!-- Header -->
    <header class="app-header">
        <button type="button" id="btn-back" class="btn-icon" aria-label="<?= __('ui.back') ?>">
            <svg viewBox="0 0 24 24"><path d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/></svg>
        </button>
        <h1 class="header-title"><?= __('contacts.title') ?></h1>
        <button type="button" id="btn-add-contact" class="btn-icon btn-primary-icon" aria-label="<?= __('contacts.add') ?>">
            <svg viewBox="0 0 24 24"><path d="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z"/></svg>
        </button>
    </header>

    <!-- Info box -->
    <div class="info-box">
        <svg class="info-icon" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-6h2v6zm0-8h-2V7h2v2z"/></svg>
        <p><?= __('contacts.info_text') ?></p>
    </div>

    <!-- Liste des contacts -->
    <div class="contacts-list" id="contacts-list">
        <!-- Chargement -->
        <div class="loading-state" id="loading-state">
            <div class="spinner-container">
                <svg class="spinner" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="3" fill="none" stroke-dasharray="31.4 31.4"/></svg>
            </div>
            <p><?= __('ui.loading') ?></p>
        </div>

        <!-- Etat vide -->
        <div class="empty-state hidden" id="empty-state">
            <svg class="empty-icon" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
            <h3><?= __('contacts.empty_title') ?></h3>
            <p><?= __('contacts.empty_text') ?></p>
            <button type="button" class="btn btn-primary" id="btn-add-first">
                <svg viewBox="0 0 24 24"><path d="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z"/></svg>
                <span><?= __('contacts.add_first') ?></span>
            </button>
        </div>

        <!-- Les contacts seront injectes ici par JS -->
    </div>
</div>

<!-- Modal ajout/edition contact -->
<div class="modal-overlay hidden" id="contact-modal">
    <div class="modal-content">
        <div class="modal-header">
            <h2 id="modal-title"><?= __('contacts.add') ?></h2>
            <button type="button" class="btn-modal-close" id="btn-modal-close">
                <svg viewBox="0 0 24 24"><path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/></svg>
            </button>
        </div>

        <form id="contact-form" class="modal-form" novalidate>
            <input type="hidden" id="contact-id" name="id">

            <div class="form-group">
                <label for="contact-name"><?= __('contacts.name') ?></label>
                <input type="text"
                       id="contact-name"
                       name="name"
                       class="form-input"
                       placeholder="<?= __('contacts.name_placeholder') ?>"
                       required>
                <span class="form-error" id="name-error"></span>
            </div>

            <div class="form-group">
                <label for="contact-phone"><?= __('contacts.phone') ?></label>
                <div class="input-phone-wrapper">
                    <select id="contact-phone-prefix" name="phone_prefix" class="form-select-prefix">
                        <option value="+33">+33</option>
                        <option value="+41" selected>+41</option>
                        <option value="+32">+32</option>
                        <option value="+49">+49</option>
                        <option value="+34">+34</option>
                        <option value="+39">+39</option>
                        <option value="+31">+31</option>
                        <option value="+46">+46</option>
                        <option value="+48">+48</option>
                        <option value="+30">+30</option>
                        <option value="+351">+351</option>
                    </select>
                    <input type="tel"
                           id="contact-phone"
                           name="phone"
                           class="form-input"
                           placeholder="<?= __('contacts.phone_placeholder') ?>"
                           required>
                </div>
                <span class="form-error" id="phone-error"></span>
            </div>

            <div class="form-group">
                <label for="contact-relationship"><?= __('contacts.relationship') ?></label>
                <select id="contact-relationship" name="relationship" class="form-select">
                    <option value=""><?= __('contacts.select_relationship') ?></option>
                    <option value="family"><?= __('contacts.rel_family') ?></option>
                    <option value="friend"><?= __('contacts.rel_friend') ?></option>
                    <option value="partner"><?= __('contacts.rel_partner') ?></option>
                    <option value="colleague"><?= __('contacts.rel_colleague') ?></option>
                    <option value="other"><?= __('contacts.rel_other') ?></option>
                </select>
            </div>

            <div class="form-group">
                <label class="checkbox-label">
                    <input type="checkbox" name="is_primary" id="contact-primary">
                    <span class="checkbox-custom"></span>
                    <span><?= __('contacts.is_primary') ?></span>
                </label>
                <p class="form-hint"><?= __('contacts.primary_hint') ?></p>
            </div>

            <div class="modal-actions">
                <button type="button" class="btn btn-secondary" id="btn-cancel">
                    <?= __('ui.cancel') ?>
                </button>
                <button type="submit" class="btn btn-primary" id="btn-save">
                    <span class="btn-text"><?= __('ui.save') ?></span>
                    <span class="btn-loader hidden">
                        <svg class="spinner" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="3" fill="none" stroke-dasharray="31.4 31.4"/></svg>
                    </span>
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Modal confirmation suppression -->
<div class="modal-overlay hidden" id="delete-modal">
    <div class="modal-content modal-confirm">
        <div class="modal-icon modal-icon-danger">
            <svg viewBox="0 0 24 24"><path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z"/></svg>
        </div>
        <h2><?= __('contacts.delete_title') ?></h2>
        <p id="delete-message"><?= __('contacts.delete_confirm') ?></p>
        <div class="modal-actions">
            <button type="button" class="btn btn-secondary" id="btn-delete-cancel">
                <?= __('ui.cancel') ?>
            </button>
            <button type="button" class="btn btn-danger" id="btn-delete-confirm">
                <?= __('ui.delete') ?>
            </button>
        </div>
    </div>
</div>
